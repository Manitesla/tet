name: Java Docker CI

on:
  push:
    branches:
      - main

jobs:

  build-and-run:

    runs-on: ubuntu-latest

    steps:

      - name: Checkout Code
        uses: actions/checkout@v4


      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'


      - name: Build Docker Image
        run: |
          docker build \
          -f Dockerfile.multi \
          -t java-demo .


      - name: Run Docker Container
        run: docker run --rm java-demo